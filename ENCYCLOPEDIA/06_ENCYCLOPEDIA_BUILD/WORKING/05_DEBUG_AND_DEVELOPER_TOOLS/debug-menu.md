# Debug Menu

## Как открыть

**VANILLA DEBUG (код):**

```
клик по объекту с GraphyCaller.click()
  → счётчик; на 11-м клике (clicked > 10)
  → DebugOption.DebugButton toggle
  → bug-кнопка HUD
```

Привязка UI: код не содержит слово burger. Публичные гайды: **Settings → иконка burger сверху слева, 11 раз**, затем bug **сверху справа**. **REQUIRES LIVE VALIDATION** конкретного GameObject; механика счётчика CONFIRMED.

Unlock **не пишется в save** (кроме цикла DisablePremium/TestAds — PARTIALLY CONFIRMED, ломает premium). Обычные 11 кликов = **сессия**.

Unity Editor: `Config.isEditor` сразу показывает кнопку. В релизе false.

Консоль: клавиша `~` **всегда** в хоткеях — лог, **не** командный чит, debug unlock не нужен. `clickConsole()` тем же счётчиком (гайд: дракон в settings) — REQUIRES LIVE для иконки.

Achievement `god_mode` при открытии окна debug.

## Что доступно после unlock

1. Bug HUD → окно `debug` (`UiDebugWindow`, `is_testable = false`)
2. Вкладки фильтруют кнопки debug-опций
3. New Debug Window → `DebugTool` панели
4. Overlay `DebugLayer` только пока bug-кнопка active

### Вкладки окна debug — LIVE CONFIRMED (игрок)

В живой игре у окна debug **6 вкладок**:

| # | Вкладка | Зачем (по смыслу) | Полный список кнопок |
|---|---|---|---|
| 1 | **Абсолютно все** | все debug-кнопки сразу | ещё не разобран |
| 2 | **Отладочные стрелки** | визуальные стрелки/линии на карте (пути, цели и т.п.) | ещё не разобран |
| 3 | **Карта** | оверлеи и отладка карты | ещё не разобран |
| 4 | **Курсор** | что связано с курсором / подсветкой под мышью | ещё не разобран |
| 5 | **Читы** | опции, которые ломают или ускоряют симуляцию | частично (см. ниже) |
| 6 | **Система** | включить/выключить куски симуляции | частично (см. ниже) |

Имена вкладок — с экрана игрока.

### Каталог кнопок по вкладкам (честная раскладка)

Легенда размещения:
- **Вкладка подтверждена** — в базе прямо сказано, где кнопка
- **По смыслу** — логично сюда, но LIVE-скрин вкладки ещё не сверяли
- **Вкладка неизвестна** — кнопка/инструмент есть, вкладка не доказана

Полный dump всех кнопок с экрана — всё ещё **OPEN**. Ниже только то, что уже есть в базе. Не выдумывать лишние имена.

#### 1. Абсолютно все

Не отдельный набор. Показывает **все** debug-кнопки сразу. Смотри списки вкладок 2–6.

#### 2. Отладочные стрелки — по смыслу

| Имя | Перевод | Что делает |
|---|---|---|
| Overlay стрелок целей (имя кнопки на UI не зафиксировано) | стрелки целей | рисует стрелки к целям боя / AI; рядом полезен tooltip AI (`fighting`) |

#### 3. Карта — почти пусто в базе

Отдельных кнопок с подтверждённой вкладкой «Карта» **нет**.  
Известны связанные вещи, но вкладка не доказана: оверлеи города (capture / loyalty / city_jobs), World Laws tool в debug, `DrawCitizenJobIcons` (иконки работ горожан на карте).

#### 4. Курсор

| Имя | Перевод | Вкладка | Что делает |
|---|---|---|---|
| MakeUnitsFollowCursor | юниты за курсором | по смыслу (Курсор или Читы) | юниты бегут за мышкой — чит поведения |
| Actor AI | AI актёра | неизвестна (по смыслу Курсор) | под курсором: job, next task, task, action |
| Unit Info | инфо юнита | неизвестна (по смыслу Курсор) | job, task, HP, город, кого бьёт, путь |
| DebugTooltipActorAI | подсказка AI | неизвестна | wait, task, action, job, citizen_job, profession; **не** показывает id текущего Decision |

#### 5. Читы — подтверждённый набор читов (вкладка по смыслу)

| Имя | Перевод | Что делает |
|---|---|---|
| SonicSpeed | сверхскорость | мир ×40; ломает «честное» время |
| FastSpawn | быстрый спавн | убирает обычную задержку спавна |
| UltraFastSpawn | сверхбыстрый спавн | ещё агрессивнее убирает задержку |
| UnlockAllTraits | открыть все черты | снимает замки с черт в редакторах; **не** выдаёт черту выбранному NPC |
| UnlockAllEquipment | открыть всё снаряжение | то же для экипировки |
| UnlockAllGenes | открыть все гены | то же для генов |
| UnlockAllActors | открыть всех существ | то же для юнитов/ассетов |
| UnlockAllPlots | открыть все заговоры | то же для plots |
| CityInfiniteResources | бесконечные ресурсы города | склад города не кончается |
| FastConstruction | быстрая стройка | ускоряет строительство |
| FastUpgrades | быстрые апгрейды | ускоряет улучшения |
| FastPopGrowth | быстрый рост населения | ускоряет рост |
| UnlimitedHouses | без лимита домов | снимает лимит домов |
| UnlimitedZoneRange | без лимита зоны | снимает лимит зоны города |
| IgnoreDamage | игнор урона | урон не проходит |
| DisablePremium | отключить премиум | ломает премиум; часть может остаться |
| TestAds | тест рекламы | тестовая реклама; осторожно с persist |

Обычные пауза/скорость часов — **ваниль**, не чит. Trailer F7/F8 sonic — INTERNAL.

#### 6. Система — вкладка подтверждена для System*

| Имя | Перевод | Что делает |
|---|---|---|
| SystemCityTasks | системные задачи города | выкл → городские задачи перестают тикать |
| SystemUpdateUnits | системное обновление юнитов | выкл → обновление юнитов останавливается |
| System… (другие) | другие системы | в базе есть только «и похожие»; полный список кнопок вкладки **не выгружен** |
| CitizenJob* (фильтры) | фильтры работ горожан | вкл/выкл слоты работ; вкладка по смыслу Система или Читы — **не подтверждена** |

### Инструменты наблюдения — вкладка неизвестна

| Имя | Перевод | Что делает |
|---|---|---|
| Actor Decisions | решения актёра | веса возможных Decision; игру не меняет |
| Actor Stats | статы актёра | полный набор статов |
| ShowHiddenStats | скрытые статы | показывает скрытые статы |
| City Capture | захват города | отладка прогресса захвата |
| City Loyalty | лояльность города | отладка лояльности |
| city_jobs | работы города | слоты/работы города |
| World Laws tool | инструмент законов мира | debug-доступ к законам (не путать с ванильным окном законов) |
| DrawCitizenJobIcons | иконки работ горожан | рисует иконки CitizenJob |

Last Decision во вкладке Mind юнита — **ваниль**, не debug.  
Гены одного NPC в debug нет — смотри редактор подвида.

## Что НЕ является ванильной игрой

Весь этот раздел после unlock. Не отвечать «игрок может SonicSpeed» без пометки DEBUG.

Консоль ≠ Lua. `initDebugHotkeys` (Ctrl+V budding, N kill, PageUp maps) **нигде не вызывается** — INTERNAL.

`Config.editor_maxim` и подобные — false, не игрок.

## MOD_ONLY

Вкладка PowerBox, EditResources, army create — **не** окно `debug`.

## LIMITATIONS

Нет кнопки debug «выдать trait этому NPC».  
Состав **6 вкладок** — LIVE CONFIRMED. Полные списки кнопок по вкладкам — ещё OPEN.  
Цикл clearDebugOnStart vs premiumDisabled — PARTIALLY CONFIRMED.

## RELATED SYSTEMS

[AI просмотр](../03_NPC_AND_AI/ai-pipeline.md) · [Уровни UI](../01_INTERFACE_AND_CONTROL/access-levels.md)
